services:
  - type: web
    name: ranassist-ai
    env: docker                  # ← uses your Dockerfile (has Node + Python)
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false              # ← you must paste this in the Render dashboard manually
