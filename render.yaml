services:
  - type: web
    name: ranassist-ai
    env: python
    buildCommand: |
      cd frontend
      npm ci
      npm run build
      cd ../backend
      pip install -r requirements.txt
    startCommand: gunicorn backend.app:app --bind 0.0.0.0:$PORT --workers 4
    envVars:
      - key: GROQ_API_KEY
        sync: false
