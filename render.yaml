services:
  - type: web
    name: ranassist-ai
    env: python
    buildCommand: |
      pip install -r backend/requirements.txt
      cd frontend
      npm ci
      npm run build
    startCommand: python -m gunicorn backend.app:app --bind 0.0.0.0:$PORT --workers 1
    envVars:
      - key: GROQ_API_KEY
        sync: false
