services:
  - type: web
    name: ranassist-ai
    env: python
    buildCommand: |
      cd frontend
      npm ci
      npm run build
      cd ../backend
      pip install -r requirements.txt
    startCommand: python backend/app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
